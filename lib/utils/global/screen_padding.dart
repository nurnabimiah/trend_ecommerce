import 'package:flutter/material.dart';

EdgeInsets screenPadding({double leftPadding = 16, double rightPadding = 16, double topPadding = 0, double bottomPadding = 0}) {
  return  EdgeInsets.only(left: leftPadding, right: rightPadding, top: topPadding, bottom: bottomPadding);
}